const Loading = () => {

    return (
        <div className="spinner-container">
            <p>Loading...</p>
          <div className="loading-spinner"></div>
        </div>
      );

}

export default Loading